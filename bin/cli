#!/usr/bin/env node

const yargs = require('yargs')
yargs.command('setup', 'Create a boilerplate interactively.', (yargs) => {}, require('./commands/setup'))
.help('h')
.showHelpOnFail(true)
.alias('h', 'help')
.alias('v', 'version')
.demandCommand()
.argv;