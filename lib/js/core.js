module.exports = mni = {
	modernizr: require('mni/modernizr').init(),
	nav: require('mni/nav').init(),
	subnav: require('mni/subnav').init(),
	lazy: require('mni/lazy').init(),
	lightbox: require('mni/lightbox').init()
};