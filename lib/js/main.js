module.exports = mni = {
	modernizr: require('modules/modernizr').init(),
	nav: require('modules/nav').init(),
	subnav: require('modules/subnav').init(),
	lazy: require('modules/lazy').init(),
	lightbox: require('modules/lightbox').init()
};